<Window x:Class="SistemaGimnacionOptimusCAI.Ventanas.NuevaMembresiaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Nueva Membresía"
        Width="540" Height="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MiDiccionario.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="LabelCampoEstilo" TargetType="TextBlock">
                <Setter Property="Foreground"  Value="{StaticResource TextSecondary}"/>
                <Setter Property="FontSize"    Value="10"/>
                <Setter Property="FontWeight"  Value="Bold"/>
                <Setter Property="FontFamily"  Value="Segoe UI Semibold, Segoe UI"/>
                <Setter Property="Margin"      Value="2,0,0,6"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource Bg1}" CornerRadius="14"
            BorderBrush="{StaticResource Border1}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="3"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{StaticResource GreenMid}" CornerRadius="14,14,0,0"/>

            <!-- Header -->
            <Grid Grid.Row="1" Margin="24,18,24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="NUEVA MEMBRESÍA" FontSize="16" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Cambio rápido de membresía del socio" FontSize="12"
                               Foreground="{StaticResource TextMuted}" Margin="0,2,0,0"/>
                </StackPanel>
                <Button Grid.Column="1" Content="✕" Width="28" Height="28"
                        Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource TextMuted}" FontSize="14" Cursor="Hand"
                        Click="btnCerrar_Click"/>
            </Grid>

            <!-- Contenido -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Padding="0,0,12,0"
                          Margin="24,16,12,0" Style="{StaticResource PanelScrollViewerStyle}">
                <StackPanel>

                    <!-- Socio -->
                    <Border Background="{StaticResource Bg2}" CornerRadius="10"
                            BorderBrush="{StaticResource Border2}" BorderThickness="0.5"
                            Padding="14,12" Margin="0,0,0,20">
                        <StackPanel Orientation="Horizontal">
                            <fa:ImageAwesome Icon="User" Foreground="{StaticResource GreenMain}"
                                             Height="18" Width="18" VerticalAlignment="Center"
                                             Margin="0,0,10,0"/>
                            <StackPanel>
                                <TextBlock x:Name="lblSocio" Foreground="{StaticResource TextPrimary}"
                                           FontSize="14" FontWeight="Bold"/>
                                <TextBlock x:Name="lblNumeroSocio" Foreground="{StaticResource GreenMain}"
                                           FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Actividad -->
                    <TextBlock Text="ACTIVIDAD *" Style="{StaticResource LabelCampoEstilo}"/>
                    <ComboBox x:Name="cmbActividad" Style="{StaticResource ComboBoxEstilo}"
                              Margin="0,0,0,14" SelectionChanged="cmbActividad_SelectionChanged">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Nombre}" FontSize="13"/>
                                    <TextBlock Text=" — $" FontSize="13" Foreground="{StaticResource TextMuted}"/>
                                    <TextBlock Text="{Binding Precio, StringFormat=N0}" FontSize="13"
                                               Foreground="{StaticResource TextMuted}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <!-- Precio -->
                    <Border x:Name="panelPrecio" Visibility="Collapsed"
                            Background="{StaticResource Bg2}" CornerRadius="10"
                            BorderBrush="{StaticResource GreenMain}" BorderThickness="1"
                            Padding="14,12" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="PRECIO A COBRAR" Foreground="{StaticResource TextMuted}"
                                           FontSize="11" FontWeight="Bold"/>
                                <TextBlock x:Name="lblActividad" Foreground="{StaticResource TextSecondary}"
                                           FontSize="12" Margin="0,2,0,0"/>
                            </StackPanel>
                            <TextBlock x:Name="lblPrecio" Grid.Column="1" FontSize="28" FontWeight="Bold"
                                       Foreground="{StaticResource GreenMain}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>

                    <!-- Vencimiento e Instructor -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="FECHA DE VENCIMIENTO" Style="{StaticResource LabelCampoEstilo}"/>
                            <DatePicker x:Name="dpVencimiento" Style="{StaticResource DatePickerEstilo}"
                                        IsEnabled="False" Margin="0,0,0,4"/>
                            <TextBlock Text="Se calcula automáticamente (un mes: mismo día del mes siguiente)"
                                       FontSize="10" FontStyle="Italic"
                                       Foreground="{StaticResource TextMuted}" Margin="0,0,0,14"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="INSTRUCTOR A CARGO (opcional)" Style="{StaticResource LabelCampoEstilo}"/>
                            <ComboBox x:Name="cmbInstructor" Style="{StaticResource ComboBoxEstilo}" Margin="0,0,0,14">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding NombreCompleto}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <!-- Método de pago -->
                    <TextBlock Text="MÉTODO DE PAGO" Style="{StaticResource LabelCampoEstilo}"/>
                    <ComboBox x:Name="cmbMetodoPago" Style="{StaticResource ComboBoxEstilo}" Margin="0,0,0,14">
                        <ComboBoxItem Content="Efectivo"      Tag="efectivo" IsSelected="True"/>
                        <ComboBoxItem Content="Transferencia" Tag="transferencia"/>
                        <ComboBoxItem Content="Tarjeta"       Tag="tarjeta"/>
                    </ComboBox>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Grid Grid.Row="3" Margin="24,14,24,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="lblError" Grid.Column="0" Text="" FontSize="11"
                           Foreground="{StaticResource ColorError}" VerticalAlignment="Center"
                           TextWrapping="Wrap" Visibility="Collapsed"/>

                <Button Grid.Column="1" Content="Cancelar" Click="btnCancelar_Click"
                        Style="{StaticResource BotonDangerLargoEstilo}"
                        Padding="18,0" Width="110" Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="COBRAR ✓" Click="btnConfirmar_Click"
                        Style="{StaticResource BotonPrincipalEstilo}"
                        Height="40" Padding="24,0" FontSize="12" Cursor="Hand"/>
            </Grid>
        </Grid>
    </Border>
</Window>
