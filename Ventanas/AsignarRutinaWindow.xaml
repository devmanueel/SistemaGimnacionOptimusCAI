<Window x:Class="SistemaGimnacionOptimusCAI.Ventanas.AsignarRutinaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Asignar Rutina"
        Width="460" Height="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/MiDiccionario.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="ComboSociosAsignarRutinaStyle" TargetType="{x:Type ComboBox}">
                <Setter Property="Foreground" Value="#F2FFF5"/>
                <Setter Property="Background" Value="#111811"/>
                <Setter Property="BorderBrush" Value="#244B2A"/>
                <Setter Property="BorderThickness" Value="1.5"/>
                <Setter Property="Height" Value="46"/>
                <Setter Property="Padding" Value="12,0"/>
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBox}">
                            <Grid>
                                <ToggleButton x:Name="toggleButton"
                                              Focusable="False"
                                              ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="{x:Type ToggleButton}">
                                            <Border x:Name="borde"
                                                    Background="#111811"
                                                    BorderBrush="#244B2A"
                                                    BorderThickness="1.5"
                                                    CornerRadius="10">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="36"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Path Grid.Column="1"
                                                          Data="M0,0 L4,4 L8,0 Z"
                                                          Fill="#F2FFF5"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="borde" Property="BorderBrush" Value="#4ADE80"/>
                                                    <Setter TargetName="borde" Property="Background" Value="#132414"/>
                                                </Trigger>
                                                <DataTrigger Binding="{Binding IsDropDownOpen, RelativeSource={RelativeSource AncestorType=ComboBox}}" Value="True">
                                                    <Setter TargetName="borde" Property="BorderBrush" Value="#4ADE80"/>
                                                    <Setter TargetName="borde" Property="BorderThickness" Value="2"/>
                                                </DataTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>

                                <ContentPresenter IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="14,0,34,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  TextElement.Foreground="#F2FFF5"/>

                                <Popup x:Name="PART_Popup"
                                       AllowsTransparency="True"
                                       IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                       Placement="Bottom"
                                       PopupAnimation="Fade">
                                    <Border Background="#111811"
                                            BorderBrush="#244B2A"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            Margin="0,2,0,0"
                                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ScrollViewer MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                                      VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ComboSociosAsignarRutinaItemStyle" TargetType="{x:Type ComboBoxItem}">
                <Setter Property="Foreground" Value="#F2FFF5"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Padding" Value="12,9"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                                            <Border x:Name="itemBorder"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}"
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter
                                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                    VerticalAlignment="Center"
                                                    TextElement.Foreground="{TemplateBinding Foreground}"/>
                                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="itemBorder" Property="Background" Value="#132414"/>
                                    <Setter Property="Foreground" Value="#4ADE80"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="itemBorder" Property="Background" Value="#1D331E"/>
                                    <Setter Property="Foreground" Value="#F2FFF5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{StaticResource Bg1}"
            CornerRadius="14"
            BorderBrush="{StaticResource Border1}"
            BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="4"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#4ADE80" CornerRadius="14,14,0,0"/>

            <Grid Grid.Row="1" Margin="22,18,22,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="lblTituloFormulario" Text="ASIGNAR RUTINA A SOCIO"
                               FontFamily="Bahnschrift SemiBold"
                               FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock x:Name="lblNombreRutina" Text=""
                               Foreground="{StaticResource GreenMain}"
                               FontSize="11" Margin="0,2,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" Content="✕"
                        Width="28" Height="28"
                        Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource TextMuted}"
                        FontSize="14" Cursor="Hand"
                        Click="btnCerrar_Click"/>
            </Grid>

            <ScrollViewer Grid.Row="2"
                          Margin="22,14,14,0"
                          Padding="0,0,8,0"
                          VerticalScrollBarVisibility="Auto"
                          Style="{StaticResource PanelScrollViewerStyle}">
                <StackPanel>
                    <TextBlock Text="SOCIO *"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="10" FontWeight="Bold" Margin="2,0,0,6"/>
                    <ComboBox x:Name="cmbSocios"
                              Style="{StaticResource ComboSociosAsignarRutinaStyle}"
                              ItemContainerStyle="{StaticResource ComboSociosAsignarRutinaItemStyle}"
                              SelectedValuePath="Id"
                              MaxDropDownHeight="190">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TextoCombo}"
                                           Foreground="#F2FFF5"
                                           FontSize="13"
                                           TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBlock Text="FECHA DE INICIO *"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="10" FontWeight="Bold" Margin="2,8,0,6"/>
                    <DatePicker x:Name="dtpFechaInicio" Style="{StaticResource DatePickerEstilo}"/>

                    <TextBlock Text="COMENTARIOS"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="10" FontWeight="Bold" Margin="2,8,0,6"/>
                    <Border Background="{StaticResource Bg2}" CornerRadius="10"
                            BorderBrush="{StaticResource Border1}" BorderThickness="1.5">
                        <TextBox x:Name="txtComentarios" Background="Transparent"
                                 BorderThickness="0" Foreground="{StaticResource TextPrimary}"
                                 CaretBrush="{StaticResource GreenMain}" FontSize="13" Padding="14,10"
                                 AcceptsReturn="True" TextWrapping="Wrap"
                                 Height="105" VerticalScrollBarVisibility="Auto"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Row="3" Margin="22,14,22,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Cancelar"
                        Style="{StaticResource BotonDangerLargoEstilo}"
                        Margin="0,0,8,0" Click="btnCancelar_Click"/>
                <Button Grid.Column="1" Content="ASIGNAR"
                        Style="{StaticResource BotonPrincipalEstilo}"
                        Margin="8,0,0,0" Click="btnAsignar_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>
